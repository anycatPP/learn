const sentence='the quick brown fox jumps over the lazy dog';
const index=4;
console.log(`the character code ${sentence.charCodeAt(index)} is equal to ${sentence.charAt(index)}`);
console.log('what the fuck'.charCodeAt(0));
